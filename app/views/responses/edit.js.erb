$('#edit-question-form').html("<%= j (render 'form') %>");
$('#edit-response-form').slideDown(350);
$('#edit-response-btn').hide();
$('#add-response-btn').hide();

$(document).mousedown(function (e)
{
  var container = $("#edit-response-form");
    if (!container.is(e.target) // if the target of the click isn't the container...
        && container.has(e.target).length === 0) // ... nor a descendant of the container
    {
      container.hide();
    }
  });

$("#submit-edit-response-btn").on('click', function() {
  $(location).attr('href', "<%= j questions_path %>");
});

