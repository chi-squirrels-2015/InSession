
<% if current_user %>

Meetup Suggestions :
<ul>
  <% if @meetups.nil? == false %>
    <% @meetups.each do |meetup| %>
    <li>
      <h3>
        <%= link_to meetup.title, question_path(meetup.id) %>
      </h3>
    </li>
    <% end %>
  <% end %>
</ul>

<% else %>
  <h2>We're glad you're here.</h2>
  <p>Learning solo can be difficult. In Session is an online community providing students with a way to learn and support each other. Through our website, students can join in-person study sessions, chat with peers and post on forums.</p>
  <p>In Session is a website created by five DevBootcamp students. As

<%= render "devise/shared/links" %>
 <% end %>

