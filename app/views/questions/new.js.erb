$(".content-wrapper #question-form").html("<%= j (render 'form') %>");
$(".content-wrapper #question-form").slideDown(350);
$('.content-wrapper #ask-question-btn').hide();

// $(document).mousedown(function (e)
// {
//   var container = $(".content-wrapper #question-form");
//     if (!container.is(e.target) // if the target of the click isn't the container...
//         && container.has(e.target).length === 0) // ... nor a descendant of the container
//     {
//       container.hide();
//     }

//   });

// $('.content-wrapper #submit-new-question-btn').last().on( "click", function(){
//   $('.content-wrapper #ask-question-btn').show();

// });
