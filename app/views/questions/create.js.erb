// remove any existing input in the form
$(".content-wrapper #new_question")[0].reset();

// add hidden class to wrapper
$(".content-wrapper #question-form").addClass("hidden");

// Show the "Add a question" link
$(".content-wrapper #ask-question-btn").show();

// Add the new question to the list
$(".content-wrapper .questiontable tr:first" ).after("<%= j (render 'row') %>")
